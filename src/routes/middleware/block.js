"use strict"

const validateBlock = (req, res, next) => {
  next()
}

const checkPixels = (req, res, next) => {
  next()
}

const updatePixel = (req, res, next) => {
  next()
}

module.exports = {
  validateBlock
}
